<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">
    <title>repl.it express</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.4.1/jquery.min.js"></script>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
  <div id="container">    
    <a href="/"><button class="btn btn-primary">Back to List</button></a>
    <a href="/edit/<%= meme._id %>"><button class="btn btn-primary">Edit Meme</button></a>
    <h3><%= meme.title %></h3><br>
    <h3>Submitted By: <%= meme.submittedBy %></h3><br>
    Am I Spicy? <%= meme.isSpicy %><br>
    <img src="<%= meme.image %>"><br><br>
    
    <form action="/comment/<%= meme._id %>" method="POST">
      <select name="score">
        <option value="5">A+</option>
        <option value="4">Solid</option>
        <option value="3">Tee Hee</option>
        <option value="2">Meh</option>
        <option value="1">Weak</option>
        <option value="0">wt heck?</option>
      </select>
      <input name="comment" size="40" placeholder="Comment">
      <input name="name" placeholder="Name">
      <input type="submit" class="btn btn-warning">
    </form>
  
    <% for(var i = meme.comments.length-1; i >= 0 ; i--) { %>
      <%= meme.comments[i].description %> - 
      <%= meme.comments[i].comment %> - 
      <%= meme.comments[i].name %><br>
    <% } %>
  
  </div>

  </body>
</html>